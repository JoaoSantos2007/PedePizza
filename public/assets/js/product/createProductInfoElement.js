const createProductInfoElement = () => {
  const productInfoElement = document.createElement('div');
  productInfoElement.classList.add('product__info');

  return productInfoElement;
};

export default createProductInfoElement;
